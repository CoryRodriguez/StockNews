# ── Auth ──────────────────────────────────────────────────
# Set this to a strong random string (e.g. openssl rand -hex 32)
JWT_SECRET=change_me_to_a_random_secret

# Dashboard password (set on first login via /setup)
# Leave blank — you will create it on first visit to the site.

# ── RTPR.io ───────────────────────────────────────────────
RTPR_API_KEY=your_rtpr_api_key_here

# ── Alpaca Markets ────────────────────────────────────────
ALPACA_API_KEY=your_alpaca_key_id_here
ALPACA_API_SECRET=your_alpaca_secret_here

# ── Database ──────────────────────────────────────────────
DATABASE_URL=postgresql://dtdash:dtdash@postgres:5432/dtdash

# ── Server ────────────────────────────────────────────────
PORT=3001
NODE_ENV=production
